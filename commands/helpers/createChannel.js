
/**
 * Create channel text name
 * @param {*} gameName 
 * @param {*} userName 
 * @param {*} date 
 */
async function createChannels(gameName, userName, date) {
    console.log(gameName + '-' + userName + '-' + date)
}

exports.createChannels = createChannels;

